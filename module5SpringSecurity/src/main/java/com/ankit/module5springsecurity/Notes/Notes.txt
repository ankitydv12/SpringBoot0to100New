Commit: 5.6 SignUp with authentication

Create a authController :
    API : signup(SigupDTO signup) that call the userServiceImp
          SingupDTO - email , password , name
    UserServiceImp :- Return BadcredentialException if the email is already exits
                   :- save the User Enity and return it
                   :- Encrypt the password using password encoder
    UserDTO : add password to attribute

In WebSecurityConfig -> securityFilterChain
    allow the "/auth/**" , comment the forlogin the
